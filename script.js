function continu()
{
    let div_a_lire = document.getElementById('id_welcome');
    div_a_lire.innerHTML = "<h2>Et c'est parti, à toi de jouer!</h2>";

        // alert(document.querySelector('input[id="flexRadioDefault1"]:checked').value);
    
        alert(document.querySelector('input[name="sex"]:checked').value);
}
